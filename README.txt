TODO

[ ] type=daemon auto enable on install? yes.

[ ] Config.groups + Group
[ ] Config.env + variable interpolation?

[ ] Service. max runtime?
[ ] Service. start type (calendar, interval, daemon)
    interval=.... first_interval=....
    calendar=....

[ ] plugins
    nginx?

sudo -H pip3 install --update git+https://github.com/mo22/control
