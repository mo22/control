# example file
version: https://github.com/mo22/control
name: example

services:

    top:
        shell: |
            while true; do
                date
                ps waux
                sleep 10
            done
        type: daemon

    t1:
        shell: |
            while true; do
                date
                sleep 10
            done

    cpustress:
        run: stress -c 2
        systemd: CPUQuota=10%
        # this works

    test-path:
        run: date

    test-js:
        cmd: test.js

    test-py:
        cmd: test.py
