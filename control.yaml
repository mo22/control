# example file
version: https://github.com/mo22/control
name: example

services:

    top:
        shell: |
            while true; do
                date
                ps waux
                sleep 10
            done

    test-path:
        run: date

    test-js:
        cmd: test.js

    test-py:
        cmd: test.py
